[build]
dockerfile = "Dockerfile.cnn"
context = "."

[deploy]
startCommand = "sh -c 'streamlit run app.py --server.port=${PORT:-8501} --server.address=0.0.0.0 --logger.level=error --client.showErrorDetails=false'"
